Day 4 — Resources & Attributes

Resource represents real infrastructure.

Each resource has attributes.
Example:
aws_s3_bucket.example.id

Terraform automatically builds a dependency graph.
Order of resources in files does not matter.
Dependencies are inferred automatically.
