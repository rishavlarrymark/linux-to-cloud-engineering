Day 5 — Outputs & ARN

Outputs expose values after Terraform runs.

ARN (Amazon Resource Name):
- Uniquely identifies AWS resources
- Used heavily in IAM policies

Outputs are used to:
- Share values
- Debug infrastructure
- Connect modules
